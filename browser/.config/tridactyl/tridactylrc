" Tridactyl config
"
" Simon H Moore
"
" Required tridactly-native to be installed.
" Run `installnative` in tridactyl.

"
" Settings
"

" Color theme to use for menu
colors dark

" Characters to use for hints
set hintchars hjklasdfgyuiopqwertnmzxcb

"
" Bind Keys
"

" Find
bind / fillcmdline find
bind ? fillcmdline find -?
bind n findnext 1
bind N findnext -1

# Mute
bind m mute toggle

" Run mpv on links
bind v hint -JW mpvsafe

" Run mpv on current URL
bind V composite get_current_url | mpvsafe

" Git{Hub,Lab} git clone via SSH yank
bind yg composite js "git clone " + document.location.href.replace(/https?:\/\//,"git@").replace("/",":").replace(/$/,".git") | clipboard yank
